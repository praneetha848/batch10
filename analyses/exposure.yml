version: 2

exposures:
  - name: weekly_metrics
    label: saisri by the Week
    type: dashboard
    maturity: high
    description: 
      For more exposure about project details
    depends_on:
       - ref('order_append')
       - ref('order_merge')
       - ref('orders')
       - ref('final_business')
       - ref('seed_emp_data')
       - ref('snap_orders_check')
       - source('datafeed_shared_schema','raw_product')
       - source('datafeed_shared_schema','raw_customerdata')
       - source('datafeed_shared_schema','emp_sal')
    
    owner:
       name: saisri
       email: snowflaketraining01@gmail.com
